spring.application.name=gate

server.port=8123

# CONFIGURACAO BASICA DO SPRING
spring.datasource.url=jdbc:sqlserver://10.1.1.4:1433;databaseName=BM;encrypt=false
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
spring.datasource.username=BM
spring.datasource.password=bm0941

# DESABILITAR FLYWAY EXPLICITAMENTE
spring.flyway.enabled=false

# HIBERNATE - CRIAR TABELAS AUTOMATICAMENTE
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect

# MOSTRAR AS INSTRUCAES SQL EXECUTADAS NO BANCO DE DADOS
# Apenas em desenvolvimento.  Em producao melhor remover
spring.jpa.show-sql=false

server.error.include-message=always
server.error.include-stacktrace=always
server.error.include-binding-errors=always
